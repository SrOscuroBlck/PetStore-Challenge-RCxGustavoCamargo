# API Reference

This document is populated as GraphQL operations are implemented — each query, mutation, and subscription is documented here only once it exists in the code. It is intentionally empty until the first operation is built.
