// Package listing holds the merchant pet-listing use cases: creating and
// removing a store's pets and querying its sold and unsold listings. Each store
// scope is supplied by the caller from the authenticated identity, never from
// client input.
package listing
