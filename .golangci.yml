version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - gosec
    - misspell
    - rowserrcheck
    - sqlclosecheck
    - unconvert
  exclusions:
    generated: lax
    paths:
      # The React app (and its node_modules, which ships incidental Go files) is
      # not part of the Go module's lint surface.
      - frontend
    rules:
      - path: _test\.go
        linters:
          - gosec

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - roboticCrewChallenge
